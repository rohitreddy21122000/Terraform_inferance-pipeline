# Production Environment Configuration

# S3 Bucket Names (will be suffixed with timestamp)
# These are base names, actual names will have timestamp appended

# EventBridge credentials - set via GitHub Secrets
# eventbridge_username = "prod-user"
# eventbridge_password = "set-in-github-secrets"

# Environment-specific tags
# Tags will be merged with module defaults
